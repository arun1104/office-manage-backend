export enum LeaveStatus {
    APPLIED = 'Applied',
    APPROVED = 'Approved',
    CANCELLED = 'Cancelled',
    REJECTED = 'Rejected',
}

export enum LeaveDuration {
    FULL_DAY = 'Full Day',
    HALF_DAY = 'Half Day',
}

export enum LeaveHalfDayPeriod {
    FIRST_HALF = 'First Half',
    SECOND_HALF = 'Second Half',
}

export enum LeaveAction {
    APPROVE = 'approve',
    REJECT = 'reject',
}